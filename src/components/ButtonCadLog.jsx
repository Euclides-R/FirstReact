import React from "react";

export default (props) => (
  <>
    <div className="cad__div">
      <a href="#">
        <i className="fas fa-user-plus"></i> {props.title}
      </a>
    </div>
  </>
);
